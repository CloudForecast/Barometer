apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudforecast-agent.fullname" . }}
  labels:
    {{- include "cloudforecast-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloudforecast-agent.selectorLabels" . | nindent 4 }}
