apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloudforecast-agent.fullname" . }}-test-connection"
  labels:
    {{- include "cloudforecast-agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloudforecast-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
